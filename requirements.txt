# Core dependencies (required for CLI)
openai>=1.0.0
python-dotenv>=1.0.0

# Slack dependencies (only needed for app.py)
slack-bolt>=1.18.0

# FastAPI for Databricks Apps deployment
fastapi>=0.104.1
uvicorn>=0.24.0
httpx>=0.25.0
pydantic>=2.4.2

# Token counting and metrics
tiktoken>=0.5.1
